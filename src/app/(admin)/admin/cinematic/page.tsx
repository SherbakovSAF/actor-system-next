import LabelUI from "@/components/ui/label.ui";

const AdminCinematicPage = async () => {
  return (
    <main>
      <LabelUI value="История съёмок">Тут съёмки</LabelUI>
    </main>
  );
};

export default AdminCinematicPage;
